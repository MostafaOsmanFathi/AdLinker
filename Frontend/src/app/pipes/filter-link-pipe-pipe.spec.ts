import { FilterLinkPipePipe } from './filter-link-pipe-pipe';

describe('FilterLinkPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLinkPipePipe();
    expect(pipe).toBeTruthy();
  });
});
